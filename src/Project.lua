-- This is an empty shell for now, but provides docs on what each key does

return {
	Name = "Tabby",

	Fragments = {
		-- define fragment names here by linking them to module scripts
		-- main = script.Parent.Fragments.main
	},

	FragmentGroups = {
		-- define groups that could be ran, uses fragment names defined above
		-- main = {"main"}
	},

	-- point to a group that will be ran
	ActiveGroup = "main"
}